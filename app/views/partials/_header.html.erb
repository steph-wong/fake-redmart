<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">

            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to "Project", home_path, class:"navbar-brand" %>
        </div>

        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="<%= btn_active('/home') %>">
                    <%= link_to "Home", home_path %>
                </li>
                <li class="<%= btn_active('/about') %>">
                    <%= link_to "About", about_path %>
                </li>
                <% if logged_in? %>
                <li class="<%= btn_active('/product') %>">
                    <%= link_to "Products", products_path %>
                </li>

            <% else %>
                <li class="<%= btn_active('/register') %>">
                    <%= link_to "Register", registers_path %>
                </li>
                <% end %>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <% if logged_in? %>
                <li class="<%= btn_active('/user') %>">
                    <%= link_to "My Account", current_user %>
                </li>
                <li class="<%= btn_active('/logout') %>">
                    <%= link_to "Log out", logout_path, method: "delete" %>
                </li>
            <% else %>
                <li class="<%= btn_active('/login') %>">
                    <%= link_to "Login", login_path %>
                </li>
            </ul>
            <% end %>


            <!-- <div class="nav navbar-form navbar-right">
                <div class="form-group">
                    <%= form_for(:session, url: login_path) do |f| %>
                    <%= f.email_field :email, placeholder: 'email', class: 'form-control' %>
                </div>
                <div class="form-group">
                    <%= f.password_field :password, placeholder: 'Password', class: 'form-control' %>
                </div>
                <button type="submit" class="btn btn-success">Sign in</button>
                <% end %>
            </div>
        </div> -->

    </div>
</nav>
</div>
